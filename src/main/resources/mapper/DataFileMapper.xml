<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ydh.dao.DataFileDao">
    <select id="findByID" resultType="com.ydh.model.DataFile">
        select id as id
        ,name as name
        ,file_path as filePath
        ,pid as pid
        ,is_directory as isDirectory
        from exec_data_file  where id=#{id}
    </select>
    <select id="queryAll" resultType="com.ydh.model.DataFile">
        select id as id
               ,name as name
               ,file_path as filePath
               ,pid as pid
               ,is_directory as isDirectory
        from exec_data_file ORDER BY name
    </select>
    <select id="queryDataFile" resultType="com.ydh.model.DataFile" parameterType="java.lang.Integer">
        select id as id
        ,name as name
        ,file_path as filePath
        ,pid as pid
        ,is_directory as isDirectory
        from exec_data_file
        where 1=1
        <if test="_parameter!=null">
            and pid=#{pid}
        </if>
        ORDER BY name
    </select>
    <insert id="addDataFile" useGeneratedKeys="true" keyProperty="id" parameterType="com.ydh.model.DataFile">
        INSERT INTO exec_data_file(name,file_path,pid,is_directory) VALUES(#{name},#{filePath},#{pid},#{isDirectory})
    </insert>

    <update id="modifyDataFile" parameterType="com.ydh.model.DataFile">
        update exec_data_file set pid=#{pid},name=#{name} where id=#{id}
    </update>

    <delete id="deleteByID" >
        delete from exec_data_file where id=#{id}
    </delete>
</mapper>